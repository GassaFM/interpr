function sum (id, pr, n, a):
	lo := id * n / pr
	hi := (id + 1) * n / pr

	s := 0
	for i := hi - 1 downto lo:
		s += a[i]
	send (0, s)

	if id == 0:
		r := 0
		for k := pr - 1 downto 0:
			r += receive (k)
		print (r)

	b := array (hi - lo)
